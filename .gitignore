# Data + generated artifacts we don't want to track as source
data/
.epg/
epg_build.log
refresh.log

# Local-only, never published (e.g. privately validated re-streams)
local/

# Secrets — never commit
.env
*.pat
*.token
secrets*

# OS
.DS_Store

# Do not publish pasted third-party/subscription playlists
*.pdf

# transient Action artifact
health-report.md
logs/
guide.xml
