name: Refresh EPG (guide.xml)

# Program data changes daily, so we rebuild guide.xml from the committed
# epg.channels.xml. This is location-independent, so a GitHub runner is fine.
# NOTE: playlist.m3u is curated + geo-validated locally (from your location) and
# is not rebuilt here — see README.
#
# The local macOS launchd job (scripts/auto-build.sh) is the PRIMARY builder and
# already produces the EPG from India. This workflow is a manual-only failsafe
# for when that Mac is offline for a while; its daily schedule is disabled to
# avoid double-push churn. Trigger it from the Actions tab when needed.

on:
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: refresh-epg
  cancel-in-progress: true

jobs:
  epg:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Build guide.xml from epg.channels.xml
        run: ./build_epg.sh 15 3

      - name: Regenerate site data (docs/channels.json)
        run: python3 gen_site.py

      - name: Commit refreshed guide
        run: |
          git config user.name  "epg-bot"
          git config user.email "epg-bot@users.noreply.github.com"
          git add guide.xml.gz docs/channels.json
          if ! git diff --cached --quiet; then
            git commit -m "chore: refresh EPG guide.xml ($(date -u +%Y-%m-%d))"
            git push
          else
            echo "guide unchanged"
          fi
